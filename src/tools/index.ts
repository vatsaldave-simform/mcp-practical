import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

export function registerTools(server: McpServer): void {
  // Import and call individual tool register functions here.
  // Example:
  //   import { register as registerEcho } from './echo.js';
  //   registerEcho(server);
}
